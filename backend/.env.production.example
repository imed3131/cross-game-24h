# Production Environment Variables for Neon + Render + Vercel

# Database (Neon PostgreSQL)
# Replace with your actual Neon database connection string
DATABASE_URL="postgresql://username:password@host.neon.tech/dbname?sslmode=require"

# JWT Secret (generate a strong secret for production)
JWT_SECRET="your-super-secure-jwt-secret-for-production"

# Admin Secret Code (change this to your desired admin code)
ADMIN_CODE="your-admin-secret-code"

# CORS Origins (add your Vercel frontend URL)
CORS_ORIGINS="https://your-app.vercel.app,http://localhost:3000"

# Port (Render will provide this automatically)
PORT=3001
